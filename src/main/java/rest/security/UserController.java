package rest.security;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.ResponseBody;
import rest.domain.Happening;
import rest.service.UserRepository;

import java.util.List;

/**
 * Created by Alex on 17.11.2014.
 * TODO: Update user Information Endpoint
 */
public class UserController {

    /*UserRepository userRepository;
    @RequestMapping(value="/users/{userID}/invites",method = RequestMethod.GET)
    public @ResponseBody
    List<Happening> showHappenings() {
        //TODO: only visible Events
        return (List)happeningRepository.findAll();
    }*/
}
